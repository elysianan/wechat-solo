# WeChat Solo

A single-user WeChat-style chat client built with Vite + React + TypeScript + Tailwind CSS.

## Scripts

- `npm run dev` — start development server
- `npm run build` — build for production
- `npm test` — run tests
