# WeChat Solo

一个高保真还原微信核心体验、并由多 Agent 人设驱动的单机社交模拟器。

## 在线 Demo

**https://elysianan.github.io/wechat-solo/**

纯前端应用,数据存于浏览器 IndexedDB,首次打开自动生成种子数据(5 位人设好友 + 群聊 + 朋友圈)。

## 产品定位

WeChat Solo 用于展示 AI 产品助理的核心能力：

- 复杂 C 端产品的信息架构与交互还原
- AI Agent 人设设计与规则驱动回复
- AI 工具（Claude Code）系统落地产品想法

## 技术栈

- React 19 + TypeScript
- Vite
- Tailwind CSS
- Zustand
- Dexie.js (IndexedDB)
- lucide-react

## 本地运行

```bash
npm install
npm run dev
```

## 测试

```bash
npm test
```

## 当前阶段

Sprint 7 已完成：消息类型补全（v1.7.0）。

- 消息模型升级为 discriminated union：text / image / voice / redpacket / transfer / location。
- 图片消息：支持本地图片选择（URL.createObjectURL），气泡内直接渲染。
- 语音消息：按住说话生成语音消息，气泡显示时长。
- 红包消息：输入金额与祝福语后发送，气泡显示红包卡片与领取状态。
- 输入区工具菜单：+ 按钮展开图片 / 语音 / 红包入口，替换原有的演示 Toast。
- 聊天列表预览：非文本消息显示 [图片] / [语音] / [微信红包] 等占位文案。
- 种子数据增加图片 / 语音 / 红包样例，打开 Demo 即可看到效果。
- 254 个测试全部通过；IndexedDB 版本升级保证旧数据平滑迁移。

## 后续计划

- Sprint 8：视觉走查，对照真微信逐项检查布局、颜色、动效还原度。

Sprint 0 已完成：项目骨架与数据地基。

Sprint 1 已完成：聊天核心流程。

- 聊天列表：显示头像、昵称、最后消息预览、时间、未读数。
- 聊天详情：顶部返回、消息气泡、底部输入框。
- 发送文字消息：输入后点击发送或按回车，消息写入 IndexedDB。
- 消息状态图标：单灰勾 / 双灰勾 / 双绿勾 UI。
- 页面转场：聊天详情从右侧滑入，返回时向左滑出。
- 演示模式：工具面板按钮点击弹出 Toast 提示。

Sprint 2 已完成：Agent 引擎与消息状态。

- 规则驱动的 Agent 回复引擎（5 个独立人设）。
- “对方正在输入…”状态与回复延迟模拟。
- 已读状态自动触发。

Sprint 3 已完成：通讯录与发现页。

- 通讯录列表、拼音搜索、字母索引。
- 好友资料页（只读 + 发消息跳转）。
- 发现页入口矩阵（朋友圈可用，其余演示 Toast）。
- 朋友圈列表、点赞、评论，持久化到 IndexedDB。

Sprint 4 已完成：个人中心与最终打磨（v1.4.0）。

- 「我」页面：个人信息卡 + 支付 / 收藏 / 相册 / 卡包 / 表情 / 设置菜单分组。
- 个人信息编辑：昵称、微信号、地区、签名可编辑并持久化。
- 支付页 Mock：余额卡片 + 六宫格功能入口（演示 Toast）。
- 设置页：深色模式（CSS 变量主题，全局零改动换肤）、声音 / 震动开关、关于页。
- 底部水印「WeChat Solo Demo · 仅供演示」。

Sprint 5 已完成：群聊与联系人标签（v1.5.0）。

- 群聊：2 个种子群（幸福一家人 / 产品研发群），群内气泡显示发言者昵称。
- 群内 Agent 调度：@提及必回（引擎 forceReply 跳过已读不回）；无 @ 时每人按人设 `groupReplyChance` 独立接话。
- @成员选择器：群聊输入框 @ 按钮，选择后按光标位置插入。
- 群资料页（成员头像网格）+ 通讯录「群聊」入口。
- 联系人标签：独立 tags 表（允许空标签），标签 CRUD + 成员管理，全部持久化。
- 180 个测试全部通过（含 11 例布局契约测试守护滚动布局）。

Sprint 6 已完成：对话真实感冲刺（v1.6.0）。

- 规则库扩容：5 人设从 13 条规则扩至 82 条，按人设拆分独立文件，每人 15~25 条规则、5~8 条主动话题。
- 上下文感知：回复可回引用户消息关键词（`{keyword}` 模板）；`context` 门槛让规则只在相关对话背景下命中。
- 防重复：session 内台词与模板原文双重去重；`maxUsageInSession` 限制规则使用次数。
- 时段感知：早/中/晚/深夜四档规则（深夜「这么晚还不睡？」类台词辨识度最高）。
- 多轮剧情链：妈妈催婚 / 阿杰约饭 / 房东催租三条剧本，支持推进、跑题拉回与脱离，进度持久化。
- 好友主动发起：按人设权重加权选人，全局 90s + 单人 5 分钟双冷却，话题防重复。
- 236 个测试全部通过；规则库版本机制保证旧浏览器数据平滑升级、聊天记录保留。
